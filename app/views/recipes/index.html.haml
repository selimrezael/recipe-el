%h1 List of Recipes

- @recipes.each do | recipe |
  = link_to recipe do
    = image_tag recipe.image.url(:thumb, class: "recipe_image")
  %h2= link_to recipe.title, recipe
  
= link_to "Create Recipe", new_recipe_path, class: 'btn btn-default'

